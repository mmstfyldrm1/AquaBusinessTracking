@using DTOLayer.Dtos.MachineStopDtos

@{
    ViewData["Title"] = "GetMachineStopList";
    Layout = "~/Views/_UserLayout/Index.cshtml";
    int counter = 0;
}
@model List<MachineStopDto>

<h6 class="mb-0 text-uppercase">Makine Duruş Takip Sistemi</h6>
<hr />

<div class="card">
    <div class="card-body">

        <div class="table-responsive">
            <table id="example2" class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Başlangıç Saati</th>
                        <th>Bitiş Saati</th>
                        <th>Süre Kaybı (dk)</th>
                        <th>Kopma Yeri</th>
                        <th>Duruş Nedeni</th>
                        <th>Açıklama</th>
                        <th>Vardiya</th>
                        <th>Kayıt Tarihi</th>
                        <th>Kayıt Eden</th>
                        <th>Güncelleme Tarihi</th>
                        <th>Güncelle</th>
                        <th>Sil</th>
                    </tr>
                </thead>

                <tbody>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@counter</td>

                            <td>
                                @(item.StartTime?.ToString("dd.MM.yyyy HH:mm") ?? "-")
                            </td>

                            <td>
                                @(item.EndTime?.ToString("dd.MM.yyyy HH:mm") ?? "-")
                            </td>

                            <td>
                                @(item.DowntimeDuration ?? 0)
                            </td>

                            <td>
                                @item.BreakLocation
                            </td>

                            <td>
                                @item.DowntimeReason
                            </td>

                            <td>
                                @item.Explanation
                            </td>

                            <td>
                                @item.ShiftName
                            </td>

                            <td>
                                @(item.InsertDate?.ToString("dd.MM.yyyy HH:mm") ?? "-")
                            </td>

                            <td>
                                @item.CreatedByName
                            </td>

                            <td>
                                @(item.UpdateDate?.ToString("dd.MM.yyyy HH:mm") ?? "-")
                            </td>

                            <td class="text-center">
                                <button class="btn btn-sm btn-outline-warning px-2 py-1"
                                        onclick="openModal(@item.RecId)">
                                    <i class="bi bi-pencil-square fs-6"></i>
                                </button>
                            </td>

                            <td class="text-center">
                                <form method="post"
                                      asp-action="Delete"
                                      asp-route-id="@item.RecId"
                                      style="display:inline;">

                                    <button type="submit"
                                            class="btn btn-sm btn-danger"
                                            onclick="return confirm('Kayıt silinsin mi?');">

                                        <ion-icon name="trash-outline"></ion-icon>
                                    </button>

                                </form>
                            </td>

                        </tr>

                        counter++;
                    }

                </tbody>

                <tfoot>
                    <tr>
                        <th>#</th>
                        <th>Başlangıç Saati</th>
                        <th>Bitiş Saati</th>
                        <th>Süre Kaybı (dk)</th>
                        <th>Kopma Yeri</th>
                        <th>Duruş Nedeni</th>
                        <th>Açıklama</th>
                        <th>Vardiya</th>
                        <th>Kayıt Tarihi</th>
                        <th>Kayıt Eden</th>
                        <th>Güncelleme Tarihi</th>
                        <th>Güncelle</th>
                        <th>Sil</th>
                    </tr>
                </tfoot>

            </table>
        </div>

        <button class="btn btn-primary shadow-sm mt-3"
                onclick="openModal()">

            <ion-icon name="add-circle-outline"></ion-icon>
            Yeni Kayıt Ekle

        </button>

    </div>
</div>

<div id="modalContainer"></div>


<script>
           function openModal(id = null) {
        const url = id
            ? `/MachineStop/Edit/${id}`
            : `/MachineStop/Edit`;

        fetch(url)
            .then(r => r.text())
            .then(html => {
                document.getElementById('modalContainer').innerHTML = html;
                const modalEl = document.getElementById('machineStopModal');
                const modal = new bootstrap.Modal(modalEl);

                modal.show();

                const form = modalEl.querySelector('form');
                form.addEventListener('submit', function (e) {
                    e.preventDefault();

                    fetch('/MachineStop/Edit', {
                        method: 'POST',
                        body: new FormData(this)
                    })
                    .then(r => {
                        if (r.ok) {
                            modal.hide();
                            location.reload();
                        }
                    });
                });
            });
    }
</script>
