@using DTOLayer.Dtos.AdminDashboardDtos
@model RawMaterialsDto

<div class="page-section" id="hammadde">

    <div class="page-section" id="hammadde">

        <div class="section-header">
            <div>
                <div class="section-title">Su Arıtma &amp; Kimyasal</div>
                <div class="section-sub">Günlük tüketim ve stok hareketleri</div>
            </div>
        </div>

        <div class="row">

            <!-- Su Arıtma Ve Analiz Tüketim -->
            <div class="col-lg-6 mb-3">
                <div class="card h-100">
                    <div class="card-title p-3">
                        <i class="ti ti-file-text"></i> Su Arıtma Ve Analiz Tüketim
                    </div>
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead>
                                <tr>
                                    <th>Malzeme</th>
                                    <th>Gelen</th>
                                    <th>Sarf Edilen</th>
                                    <th>Stok</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model.WaterPreparationAndConsumption)
                                {
                                    <tr>
                                        <td>@item.InventoryName</td>
                                        <td>@item.IncomingQuantity.ToString("N2")</td>
                                        <td>@item.ConsumedQuantity.ToString("N2")</td>
                                        <td>@item.CurrentStock.ToString("N2")</td>
                                    </tr>
                                }
                            </tbody>
                            <tfoot class="table-light">
                                <tr class="fw-bold">
                                    <td>Toplam</td>
                                    <td>@Model.WaterPreparationAndConsumption.Sum(x => x.IncomingQuantity).ToString("N2")</td>
                                    <td>@Model.WaterPreparationAndConsumption.Sum(x => x.ConsumedQuantity).ToString("N2")</td>
                                    <td>-</td>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                </div>
            </div>

            <!-- Kimyasal Tüketim Kartı -->
            <div class="col-lg-6 mb-3">
                <div class="card h-100">
                    <div class="card-title p-3">
                        <i class="ti ti-flask"></i> Kimyasal Tüketim
                    </div>
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead>
                                <tr>
                                    <th>Malzeme</th>
                                    <th>Gelen</th>
                                    <th>Tüketilen</th>
                                    <th>Stok</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model.PapperMachineChemical)
                                {
                                    <tr>
                                        <td>@item.InventoryName</td>
                                        <td>@item.IncomingQuantity.ToString("N2")</td>
                                        <td>@item.ConsumedQuantity.ToString("N2")</td>
                                        <td>@item.CurrentStock.ToString("N2")</td>
                                    </tr>
                                }
                            </tbody>
                            <tfoot class="table-light">
                                <tr class="fw-bold">
                                    <td>Toplam</td>
                                    <td>@Model.PapperMachineChemical.Sum(x => x.IncomingQuantity).ToString("N2")</td>
                                    <td>@Model.PapperMachineChemical.Sum(x => x.ConsumedQuantity).ToString("N2")</td>
                                    <td>-</td>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                </div>
            </div>

        </div>

    </div>
